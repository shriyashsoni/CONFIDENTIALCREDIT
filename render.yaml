services:
  - type: web
    name: fhenix-confidential-credit
    env: node
    rootDir: .
    buildCommand: cd frontend && npm install && npm run build
    startCommand: cd frontend && npm run start
    envVars:
      - key: NODE_VERSION
        value: 20
